require 'rails_helper'

RSpec.describe Listing, type: :model do

  context "should validates title" do
  	# it { is_expected.to validate_uniqueness_of(:title) }
  	# it { is_expected.to validate_presence_of(:title) }
  end

end
